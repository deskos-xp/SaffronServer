<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saffronexplorer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ResultView"
            android:label="@string/title_activity_result_view"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".ProductSearchResults"
            android:label="@string/title_activity_product_search_results"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".productSearch"
            android:label="@string/title_activity_product_search"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".loggedIn"
            android:label="@string/title_activity_logged_in"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>